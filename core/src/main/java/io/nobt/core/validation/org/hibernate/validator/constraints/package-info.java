/**
 * Contains repackaged validations of hibernate validator in order to avoid a direct dependency on it.
 * Sadly, the annotations cannot be referenced separately.
 */
package io.nobt.core.validation.org.hibernate.validator.constraints;